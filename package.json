{
  "name": "seekr-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/generative-ai": "latest",
    "cors": "^2.x",
    "express": "^4.18.2",
    "pino": "^8.x",
    "pino-http": "^9.x"
  },
  "devDependencies": {
    "@types/cors": "^2.x",
    "@types/express": "^4.x",
    "typescript": "^5.x"
  }
}
